name: iotdevice-test-snap
base: core24
version: '0.1'
summary: Snap for snap testing
description: |
  Snap for snap testing

grade: stable 
confinement: strict

apps:
  hello:
    command: hello.sh

parts:
  hello:
    source: bin
    plugin: dump
